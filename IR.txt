IR

Expression: resultat typé sur la stack, peut etre empty

	(add (int 42) (int 51))
		
	(if condExpr trueExpr falseExpr)
	
	(block (exprs ...)

	(set_local 0 expr)
	
	(local 0)
	
	(loop expr)
	
	(exit)
	
	(return (exprs...))
	
	(call (exprs...))
	


